% Basic layout
\documentclass[
  paper = 6in:9in,
  div = 16, % smallest margins
  12pt
]{scrbook}

% Encoding
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}

% Use Alegreya font super family
\usepackage{Alegreya}
\usepackage[black]{AlegreyaSans}

% Use Alegreya Sans Regular Italic in page headers
\setkomafont{pagehead}{\sffamily\mdseries\itshape}

% Use Latin Modern Typewriter Proportional
\renewcommand*\ttdefault{lmvtt}

% Microtypography
\usepackage[tracking = true, babel = true]{microtype}
\SetTracking{encoding = {*}, shape = sc}{40}

% Hyperlink styles
\usepackage[setpagesize = false, pdfpagelayout = TwoPageRight]{hyperref}
\usepackage{xcolor}
\hypersetup{
  colorlinks,
  linkcolor = {red!50!black},
  citecolor = {red!50!black},
  urlcolor = {blue!80!black}
}

% Arbitrary font sizes are used by titlepage.tex
\usepackage{anyfontsize}

% Use `\endnote` instead of `\footnote`
\usepackage[totoc = chapter]{enotez}

% Footnotes use roman numerals to distinguish from endnotes
\renewcommand{\thefootnote}{\roman{footnote}}

% PDF metadata
\usepackage[a-1b]{pdfx}
\title{Revolution and Counter Revolution in Spain}
\author{Felix Morrow}