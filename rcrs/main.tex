% Basic layout
\documentclass[
  12pt,
  pagesize,
  paper = 6in:9in,
  DIV = 12
]{scrbook}

% Encoding
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}

% Use Alegreya font super family
\usepackage{Alegreya}
\usepackage[black]{AlegreyaSans}

% Microtypography
\usepackage[tracking = true, babel = true]{microtype}
\SetTracking{encoding = {*}, shape = sc}{40}

% Hyperlink styles
\usepackage[setpagesize = false, pdfpagelayout = TwoPageRight]{hyperref}
\usepackage{xcolor}
\hypersetup{
	colorlinks,
	linkcolor = {red!50!black},
	citecolor = {red!50!black},
	urlcolor = {blue!80!black}
}

% Arbitrary font sizes are used by titlepage.tex
\usepackage{anyfontsize}

% Use `\endnote` instead of `\footnote`
\usepackage[totoc = chapter]{enotez}

% Footnotes use roman numerals to distinguish from endnotes
\renewcommand{\thefootnote}{\roman{footnote}}

% Drop caps
\usepackage{lettrine}

\newcommand{\lettrineA}[1]{\lettrine[lines=3, slope=4pt, findent=-3pt]{A}{#1}}
\newcommand{\lettrineF}[1]{\lettrine[lines=3, slope=-4pt, nindent=-2pt, findent=4pt]{F}{#1}}
\newcommand{\lettrineT}[1]{\lettrine[lines=3, findent=2pt, nindent=0pt]{T}{#1}}

% Blockquote styling
\usepackage{etoolbox}
\AtBeginEnvironment{quotation}{\small}
\AtBeginEnvironment{quote}{\small}

% The nowidow package provides \nowidow and \noclub
\usepackage{nowidow}

% PDF metadata
\usepackage[a-1b]{pdfx}
\title{Revolution and Counter Revolution in Spain}
\author{Felix Morrow}

\begin{document}

\frontmatter

  %\pdfbookmark[0]{Title}{title}
  %\include{titlepages}
  
  %\pdfbookmark[0]{Contents}{contents}
  %\tableofcontents

  %\include{foreword}

\mainmatter

  %\include{chapter1}
  \include{chapter2}
  %\include{chapter3}
  %\include{chapter4}
  %\include{chapter5}
  %\include{chapter6}

\backmatter

  %\printendnotes

\end{document}